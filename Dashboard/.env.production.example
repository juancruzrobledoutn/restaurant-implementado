# Copy to .env.production and fill in production values
# VITE_API_URL must NOT have a trailing slash and must NOT include /api
VITE_API_URL=https://api.yourdomain.com
VITE_WS_URL=wss://ws.yourdomain.com
