## 1. Models & Migration

- [ ] 1.1 Create `backend/rest_api/models/outbox.py` with `OutboxEvent`: `id` (BigInteger PK), `event_type` (String 100), `payload` (JSONB, use `sqlalchemy.dialects.postgresql.JSONB`), `created_at` (DateTime default `func.now()`), `processed_at` (DateTime nullable). Table `outbox_event`. Partial index `ix_outbox_pending` on `processed_at` WHERE `processed_at IS NULL`. Index on `(event_type, created_at)`.
- [ ] 1.2 Create `backend/rest_api/models/promotion.py` with 3 models: `Promotion` (tenant-scoped with `tenant_id` FK RESTRICT, `name`, `description` nullable, `price` Integer centavos, `start_date`, `start_time`, `end_date`, `end_time`, `promotion_type_id` nullable BigInteger, `AuditMixin`), `PromotionBranch` (`promotion_id` FK CASCADE, `branch_id` FK CASCADE, UniqueConstraint on the pair), `PromotionItem` (`promotion_id` FK CASCADE, `product_id` FK CASCADE, UniqueConstraint on the pair). Relationships bidirectionally linked.
- [ ] 1.3 Create `backend/rest_api/models/push_subscription.py` with `PushSubscription`: `id` (BigInteger PK), `user_id` (FK `app_user` CASCADE), `endpoint` (String 2048 UNIQUE), `p256dh_key` (String 255), `auth_key` (String 255), `AuditMixin`. Index on `user_id`. Register all new models in `backend/rest_api/models/__init__.py` so Alembic autogenerate picks them up.
- [ ] 1.4 Generate Alembic migration `010_staff_management` via `alembic revision --autogenerate -m "staff management and outbox"`. Review the generated file: ensure the 5 tables are created with correct FKs, indexes, partial index on outbox, and `UniqueConstraint` on junctions. Write explicit `downgrade()` that drops all 5 tables in reverse order. Verify chain: `down_revision` points to migration 009 (billing) or the last archived migration.
- [ ] 1.5 Run `alembic upgrade head` locally against a fresh postgres; verify all 5 tables exist with correct schemas (`\d outbox_event`, `\d promotion`, etc.). Run `alembic downgrade -1` then `upgrade head` again to confirm round-trip works.

## 2. Pydantic Schemas

- [ ] 2.1 Create `backend/rest_api/schemas/staff.py`: `StaffCreate` (email EmailStr, password SecretStr, first_name, last_name, assignments: list of `RoleAssignmentIn{branch_id, role}`), `StaffUpdate` (all optional, password SecretStr optional), `StaffOut` (id, email, first_name, last_name, is_active, assignments: list of `RoleAssignmentOut{branch_id, branch_name, role}`). Validate role is in `Roles` enum (from `shared/config/constants.py`).
- [ ] 2.2 Create `backend/rest_api/schemas/waiter_assignment.py`: `WaiterAssignmentCreate` (user_id, sector_id, date), `WaiterAssignmentOut` (id, user_id, sector_id, date, user: UserMini, sector: SectorMini), `VerifyBranchAssignmentOut` (assigned: bool, sector_id: int | None, sector_name: str | None).
- [ ] 2.3 Create `backend/rest_api/schemas/promotion.py`: `PromotionCreate` (name, description?, price: conint(ge=0), start_date, start_time, end_date, end_time, promotion_type_id?, branch_ids: list[int], product_ids: list[int]), `PromotionUpdate` (optional metadata only — no branch/product lists), `PromotionOut` (full fields + nested branches + items). Root validator: `start_date + start_time ≤ end_date + end_time`.
- [ ] 2.4 Create `backend/rest_api/schemas/push_subscription.py`: `PushSubscriptionIn` (endpoint: HttpUrl with min_length=10, p256dh_key: str min_length=10, auth_key: str min_length=10), `PushSubscriptionOut` (id, endpoint, is_active).
- [ ] 2.5 Create `backend/rest_api/schemas/outbox.py`: `OutboxEventOut` (id, event_type, payload: dict, created_at, processed_at) — used only in tests and admin debug endpoints (if any).

## 3. Domain Services

- [ ] 3.1 Create `backend/rest_api/services/outbox_service.py` with `OutboxService` (stateless module-level functions or a class with a static method). Signature: `write_event(db: Session, event_type: str, payload: dict) -> OutboxEvent`. Validate payload is JSON-serializable via `json.dumps` before `db.add`; raise `ValueError` on failure. Docstring MUST state "Caller owns the commit — use `safe_commit(db)` after business operations. The background processor is implemented in a later change."
- [ ] 3.2 Create `backend/rest_api/services/staff_service.py` with `StaffService` extending `BranchScopedService[User, StaffOut]`. Override `_apply_branch_filter` to join with `UserBranchRole`. Implement: `create_user`, `update_user`, `soft_delete_user` (ADMIN only — raise ForbiddenError otherwise), `list_users` (with `branch_id` and `role` filters + pagination), `get_by_id`, `assign_role_to_branch` (upsert on user+branch), `revoke_role_from_branch` (hard delete the `UserBranchRole`). Reuse `bcrypt_context` from `backend/shared/security/hashing.py` for password hashing. All mutations end with `safe_commit(db)`.
- [ ] 3.3 Create `backend/rest_api/services/waiter_assignment_service.py` with `WaiterAssignmentService`. Methods: `create(user_id, sector_id, date)` (validate unique + manager has access to sector.branch), `list_by_date(date, branch_id=None, sector_id=None)` with `selectinload(user, sector)`, `delete(id)` (hard delete — ephemeral per C-07), `verify_for_branch(user_id, branch_id, target_date)` returns `VerifyBranchAssignmentOut` using join `WaiterSectorAssignment → BranchSector → Branch WHERE user_id=X AND branch.id=Y AND date=target_date AND sector.is_active.is_(True)`. Order by `sector_id ASC` for deterministic first-match.
- [ ] 3.4 Create `backend/rest_api/services/promotion_service.py` with `PromotionService(BaseCRUDService[Promotion, PromotionOut])`. Methods: `create` (inserts Promotion + junction rows atomically), `update` (metadata only), `soft_delete` (ADMIN only), `list_for_tenant` (paginated), `list_for_branch` (includes expired), `link_branch` (validates `branch.tenant_id == promotion.tenant_id` via join — raises ForbiddenError on mismatch), `unlink_branch`, `link_product` (validates product tenant via `product → subcategory → category → branch → tenant_id`), `unlink_product`.
- [ ] 3.5 Create `backend/rest_api/services/push_notification_service.py` with `PushNotificationService`. Methods: `subscribe(user_id, endpoint, p256dh_key, auth_key)` using SQLAlchemy `insert().on_conflict_do_update(index_elements=['endpoint'])` (postgres dialect), `unsubscribe(user_id, endpoint)` (hard delete, scoped to user), `send_to_user(user_id, title, body, url=None)` that loads VAPID keys from `shared/config/settings.py`, iterates active subscriptions, calls `pywebpush.webpush(...)`, and on `WebPushException` with 410 sets `is_active=False`. If VAPID env missing, log WARNING via `get_logger()` and return early (fail-open). Mock `pywebpush.webpush` in tests.

## 4. Routers (HTTP Layer — thin)

- [ ] 4.1 Create `backend/rest_api/routers/admin/staff.py` with prefix `/api/admin/staff`. Endpoints: `GET /` (list with filters, paginated), `GET /{id}`, `POST /` (201), `PATCH /{id}`, `DELETE /{id}` (204), `POST /{id}/branches`, `DELETE /{id}/branches/{branch_id}`. Every endpoint uses `PermissionContext.require_management()` except DELETE user which uses `require_admin()`. Password field NEVER in any response schema.
- [ ] 4.2 Create `backend/rest_api/routers/admin/waiter_assignments.py` with prefix `/api/admin/waiter-assignments`. Endpoints: `GET /` (filters: `date`, `branch_id`, `sector_id`, `from_date`, `to_date`), `POST /` (201), `DELETE /{id}` (204). MANAGER requires branch access to the sector.
- [ ] 4.3 Create `backend/rest_api/routers/admin/promotions.py` with prefix `/api/admin/promotions`. Endpoints: `GET /` (paginated, `branch_id` filter), `GET /{id}`, `POST /`, `PATCH /{id}`, `DELETE /{id}` (ADMIN only — 403 for MANAGER), `POST /{id}/branches`, `DELETE /{id}/branches/{branch_id}`, `POST /{id}/products`, `DELETE /{id}/products/{product_id}`.
- [ ] 4.4 Create `backend/rest_api/routers/waiter/assignments.py` with prefix `/api/waiter`. Endpoint: `GET /verify-branch-assignment?branch_id={id}` (JWT, requires WAITER role — explicit role check `current_user.roles` contains `"WAITER"`, else 403). Always returns 200 with `VerifyBranchAssignmentOut`. Use `date.today()` in UTC.
- [ ] 4.5 Create `backend/rest_api/routers/waiter/notifications.py` with prefix `/api/waiter/notifications`. Endpoints: `POST /subscribe` (WAITER only, 201 or 200 on upsert), `DELETE /subscribe?endpoint={url}` (WAITER only, 204 always — idempotent).
- [ ] 4.6 Register all 5 routers in `backend/rest_api/main.py` or the router aggregator (follow existing pattern from menu/sectors/tables routers).

## 5. Dependencies & Config

- [ ] 5.1 Add `pywebpush>=2.0.0` to `backend/requirements.txt` (pulls in `cryptography`, `py-vapid`, `http_ece` transitively). Run `pip install -r requirements.txt` locally and capture the actually-resolved versions to pin.
- [ ] 5.2 Extend `backend/shared/config/settings.py` (`Settings` pydantic model) with `VAPID_PUBLIC_KEY: str = ""`, `VAPID_PRIVATE_KEY: str = ""`, `VAPID_CONTACT_EMAIL: str = ""`. Update `backend/.env.example` with placeholder values and a comment `# Generate with: vapid --gen`. Do NOT set real keys in the repo.

## 6. Tests (pytest)

- [ ] 6.1 Create `backend/tests/test_outbox_service.py`: verify `write_event` adds to session without commit; atomicity test (write event + business row, rollback on exception → both absent); JSON-serialization failure raises ValueError; processed_at partial index is used (optional — check via `EXPLAIN`).
- [ ] 6.2 Create `backend/tests/test_staff_service.py`: ADMIN creates/updates/soft-deletes user; MANAGER cannot delete (403); email uniqueness 409; password hashed (never plaintext in DB); multi-tenant isolation (tenant 2 users invisible to tenant 1 ADMIN); `assign_role_to_branch` upserts; `revoke_role_from_branch` hard-deletes.
- [ ] 6.3 Create `backend/tests/test_waiter_assignments.py`: create + unique constraint (409); list by date/branch/sector; delete; MANAGER with branch access succeeds, without access gets 403.
- [ ] 6.4 Create `backend/tests/test_verify_branch_assignment.py`: WAITER assigned returns `{assigned: true, sector_id, sector_name}`; not assigned returns `{assigned: false}`; non-existent branch returns `{assigned: false}` (no leak); ADMIN/MANAGER/KITCHEN get 403; UTC boundary test (mock `date.today()`); multiple sectors same branch → first match deterministic.
- [ ] 6.5 Create `backend/tests/test_promotion_service.py`: create with branches + products (transactional); temporal validation (start > end → 422); price negative rejected; cross-tenant link raises 403; `list_for_branch` includes expired; soft-delete does not cascade junctions; MANAGER cannot delete (403).
- [ ] 6.6 Create `backend/tests/test_push_subscription.py`: subscribe idempotent (upsert by endpoint, changes user_id); unsubscribe hard-deletes; unsubscribe non-existent returns 204 (idempotent); non-WAITER gets 403; VAPID keys missing → `send_to_user` logs WARNING and returns (fail-open, mock `get_logger` or capture with caplog); 410 response from `pywebpush` marks `is_active=False` (mock `pywebpush.webpush` to raise `WebPushException` with status 410).

## 7. Seed & Docs

- [ ] 7.1 Extend `backend/scripts/seed.py` (or the equivalent seed entrypoint from C-02): add a demo `Promotion` for tenant 1 vinculated to branch 1 and 1 product; add a `WaiterSectorAssignment` for `waiter@demo.com` in the first sector with `date=date.today()` so the smoke test works out of the box. Idempotent: re-running seed doesn't duplicate.
- [ ] 7.2 Run the full test suite `pytest backend/tests/ -q`; all new tests MUST pass. Run `ruff check backend/` and `mypy backend/rest_api/` — zero errors. Verify migration round-trip one more time (`alembic downgrade base && alembic upgrade head`).
